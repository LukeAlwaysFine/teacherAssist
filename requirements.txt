# teacherAssist 依赖

# Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# 数据库
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
aiosqlite>=0.20.0

# 认证
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# 配置管理
pydantic-settings>=2.2.0

# AI 服务（多 Provider）
anthropic>=0.30.0          # Anthropic Claude
openai>=1.0.0              # DeepSeek 及 OpenAI 兼容接口
tenacity>=8.2.0

# STT (Speech-to-Text)
pywhispercpp>=0.1.0
torch>=2.0.0

# 报告图片生成
pillow>=10.0.0

# 开发工具
ruff>=0.3.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Docker 部署
docker>=7.0.0
